import { useState } from "react";

const GradeCard = (
  grade,
  onRemoveGrade,
  onRenameGrade,
  onChangeWeight,
  onChangeValue,
  subjectId,
  PeriodID,
) => {
  return <div></div>;
};

export default GradeCard;
